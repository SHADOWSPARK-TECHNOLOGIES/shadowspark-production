# START PROMPT — shadowspark-production

This file is the Cursor-local copy of the session start prompt.

Use the repository-root `START_PROMPT.md` as the pasteable prompt. Keep both files in sync when editing.
